<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xuk.imaginary">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.xuie.imaginary.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.xuie.imaginary.gui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.xuie.imaginary.gui.gank.show.GankActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.xuie.imaginary.gui.web.WebViewActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.xuie.imaginary.gui.nets.detail.NetsOneActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>